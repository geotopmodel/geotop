include_dirs += [include_directories(['geotop',
                                      'gt_utilities',
                                      'libraries',
                                      'meteoio_plugin'])]

subdir('geotop')
subdir('gt_utilities')
subdir('libraries')
subdir('meteoio_plugin')
