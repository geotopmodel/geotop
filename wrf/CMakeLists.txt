PROJECT(wrf)

IF(COMMAND CMAKE_POLICY)
        CMAKE_POLICY(SET CMP0003 NEW)
ENDIF(COMMAND CMAKE_POLICY)

ADD_LIBRARY( wrfIO ${LINKING_MODE}
	wrfIO.cc
	wrfIO.h
)

TARGET_LINK_LIBRARIES( wrfIO
)

CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

